# Dev image: hot-reload against a bind-mounted source tree (see docker-compose.yml)
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "run", "dev"]
